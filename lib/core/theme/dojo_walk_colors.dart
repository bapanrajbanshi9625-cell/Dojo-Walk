import 'package:flutter/material.dart';

class DojoWalkColors {
  DojoWalkColors._();

  // ============================================================
  // BRAND
  // ============================================================

  /// Official Dojo Walk brand color.
  static const Color primary = Color(0xFFE86100);

  /// Darker shade for pressed / dark brand states.
  static const Color primaryDark = Color(0xFFC94F00);

  /// Soft brand background.
  static const Color primaryLight = Color(0xFFFFF0E6);

  // ============================================================
  // NEUTRAL
  // ============================================================

  static const Color black = Color(0xFF0F172A);
  static const Color dark = Color(0xFF111827);

  static const Color textPrimary = Color(0xFF1F2937);
  static const Color textSecondary = Color(0xFF6B7280);
  static const Color textTertiary = Color(0xFF9CA3AF);

  static const Color background = Color(0xFFF8FAFC);
  static const Color surface = Color(0xFFFFFFFF);

  static const Color border = Color(0xFFE5E7EB);
  static const Color divider = Color(0xFFE5E7EB);

  // ============================================================
  // BLUE — INFORMATION / LOCATION
  // ============================================================

  static const Color blue = Color(0xFF2563EB);
  static const Color blueLight = Color(0xFFEFF6FF);

  // ============================================================
  // GREEN — SUCCESS / VERIFIED / ACTIVE
  // ============================================================

  static const Color green = Color(0xFF16A34A);
  static const Color greenLight = Color(0xFFF0FDF4);

  // ============================================================
  // AMBER — WARNING / PENDING
  // ============================================================

  static const Color amber = Color(0xFFF59E0B);
  static const Color amberLight = Color(0xFFFFFBEB);

  // ============================================================
  // RED — ERROR / CANCEL / DANGER
  // ============================================================

  static const Color red = Color(0xFFDC2626);
  static const Color redLight = Color(0xFFFEF2F2);

  // ============================================================
  // SPECIAL
  // ============================================================

  static const Color white = Colors.white;
  static const Color transparent = Colors.transparent;
}
